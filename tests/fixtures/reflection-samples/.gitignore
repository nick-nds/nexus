# Only the tiny demoapp sample is committed. Larger fixtures (crm,
# largeapp) are kept locally for stress-testing the loader against real
# enterprise-scale documents but are too big for the repo.
*.json
!demoapp.json
